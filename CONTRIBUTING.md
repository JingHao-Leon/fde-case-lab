# 贡献指南

欢迎贡献。本仓库的定位是**真实 FDE 案例的可运行、可复现工程实现**，
所有贡献请围绕这个定位。

## 新增一个案例项目

1. 从《Datawhale FDE 案例 100》或你自己的真实交付中选一个场景；
2. 目录结构必须与现有案例一致：`data_gen.py`（固定种子合成数据或
   真实数据装载，注明来源与许可）/ `solution.py`（至少一个基线 + 一个
   优化方法）/ `bench.py`（输出指标表）/ `test_solution.py` / `README.md`；
3. README 必须包含：业务背景（原案例摘要）→ 技术问题 → 方案 →
   **实测结果表**（数字来自 bench 实跑）→ 诚实边界；
4. 数字红线：不许写任何 bench 跑不出来的数字；与原案例宣称数字有差距时
   如实标注根因；
5. `pytest cases/<你的案例>` 全绿后提交 PR。

## 修改现有案例

- 不允许"为了让指标变好而改验收口径"；改指标定义必须在 README 说明原因；
- 修复 bug 请附带一个能复现问题的测试。

## 技能（skills/）

新增 SKILL.md 需先在 `skills/RESEARCH.md` 的评分框架下 ≥ 20 分（一级），
或明确标注为二级候选；每个技能必须链接到可运行的参考实现。

## 风格

- Python 3.11+，依赖尽量只用 numpy / scikit-learn / pandas；
- 中文注释与文档，代码标识符用英文；
- 提交信息格式：`案例NN：一句话（关键数字变化）`。
